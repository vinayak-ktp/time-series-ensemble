[flake8]
max-line-length = 120
ignore = E501,W503,E203
exclude = venv,.venv,.git,__pycache__,build,dist

[isort]
profile = black
line_length = 100
skip = venv,.venv

[tool:pytest]
testpaths = tests
addopts = -p no:hydra
filterwarnings =
    ignore::DeprecationWarning
